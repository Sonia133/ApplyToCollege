@model College.Models.Dean
@{
    ViewBag.Title = "Edit dean";
}

<div style="display: flex; flex-direction: column; align-items: center">
    <h2>@ViewBag.Title</h2>
    <br />
    <br />

    <div class="panel panel-info" style="background-color: #d9edf7; width: 50%">
        <h3 class="panel-heading text-center">@Model.Name</h3>
        @using (Html.BeginForm(actionName: "Edit", controllerName: "Dean", method: FormMethod.Post))
        {
            <div class="form-horizontal panel-body text-center">

                @Html.HiddenFor(x => x.DeanId)

                <div class="form-group">
                    @Html.LabelFor(x => x.Name, new { @class = "control-label col-md-3" })
                    <div class="col-md-9">
                        @Html.TextBoxFor(x => x.Name, new { @class = "form-control" })
                        @Html.ValidationMessageFor(x => x.Name, "", new { @class = "text-danger" })
                    </div>
                </div>

                <div class="form-group">
                    @Html.LabelFor(x => x.Email, new { @class = "control-label col-md-3" })
                    <div class="col-md-9">
                        @Html.TextBoxFor(x => x.Email, new { @class = "form-control" })
                        @Html.ValidationMessageFor(x => x.Email, "", new { @class = "text-danger" })
                    </div>
                </div>

                <div class="form-group text-center" style="margin-right: 13%">
                    <div class="col-md-10 col-md-offset-2">
                        <input type="submit" value="Edit" class="btn btn-primary" />
                    </div>
                </div>

            </div>
        }
    </div>
</div>